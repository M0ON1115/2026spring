# 260515 수업 리뷰
1. 후기
모든 데이터를 벡터로 나타낼 수 있다는 게 흥미롭습니다. 눈과 귀를 통해 직접 취득한 정보를 처리할 수 있는 사람과 달리 숫자로 데이터를 처리하는 컴퓨터는 어떻게 사진을 인식하고 음악을 만드는 지 궁금했는데, 데이터를 벡터라는 숫자의 배열로 변환하여 수학적인 게산을 통해 처리한다는 원리를 알 수 있었습니다. 인문사회 계열의 자료들은 인간의 무작위한 개성에 따라 결과값을 예측하거나 분석하기 어려운 경우가 많은데, 벡터가 이를 분석하고 미래 전략을 도출하는 한 가지 방법이 될 수 있을 것 같습니다. AI의 논리 구조가 제가 원하는 방향과 달라 아쉬울 때가 많았는데, 행렬을 통해 벡터를 변환한다는 원리를 통해 AI를 다듬을 방법을 생각해볼 수 있었습니다. 더 깊은 수준의 선형대수학 지식과, 파인튜닝 방법을 배워 직접 AI 모델을 다듬어 보고 싶습니다. 또한 제 프로젝트에 활용할 방법도 생각해보았습니다. 현재 진행하고 있는 프로젝트에서 사용하는 ai 모델은 openai의 gpt 4.5 모델인데, 생각보다 토큰 효율이 떨어져 고민이었습니다. 이번 수업에서 배운 내용을 활용하여 질문을 벡터로 저장해둔 뒤, 새로 들어오는 질문의 벡터와의 유사도를 판단할 떄는 저렴한 모델을 사용하고 구체적인 답변을 생성할 때 고성능 모델을 사용하면 토큰을 더 효율적으로 사용할 수 있을 것 같습니다.

2. 이번에도 전체 수업 내용을 정리한 review 260515.html 파일을 업로드해두었습니다. 이전에 행렬 간의 계산을 배운 적이 있는데, 당시엔 벡터의 개념도 모호하고 행렬이 어떤 역할을 하는지 몰라서 게산 방식도 감이 오지 않았고 열과 행이 서로 다른 행렬을 계산했을 떄 또 다른 열과 행의 행렬이 나오는 결과도 설명하기 어려웠습니다. 백터와 행렬의 곱연산 방법을 알고 나니 왜 2X3 행렬은 3Xn 형태의 행렬과만 연산할 수 있는지 설명할 수 있게 되었습니다. 위와 같은 원리로 여러 행렬의 연산을 통해 AI가 작동하는 것이라 설명해주셨는데, 어떻게 서로 다른 입력을 받아 결과를 도출할 수 있는지 궁금해졌습니다. 처음에는 하나의 입력값에는 하나의 결과만이 따르는 줄 알았고, 이런 방식이라면 여러 경우 인풋에 따라 여러 정답을 내는게 말이 안 된다고 생각했습니다. 그러나 알아본 결과 하나의 입력 벡터를 받아서 여러 개의 결과 벡터를 출력하고 그 중 유사도 점수가 가장 높은 결과값을 출력하는 방식이라는것을 알 수 있었습니다. 해당 조사를 통해 프로젝트의 효율성을 높일 방법을 한 가지 더 생각해보았습니다. 제 프로젝트의 기능 중에는 사용자가 선택한 진로의 시장 적합도를 평가하는 기능이 있는데, 사용자가 평가를 시도할때마다 AI 모델을 돌리는건 토큰 부담이 너무 클 것 같았습니다. 시장 조사 결과를 사전에 벡터로 저장해 두고 사용자의 선택 벡터를 이와 비교하기만 하는 방식으로 수행하면 해당 문제를 해결할 수 있을 것 같습니다.

# 260508 수업 리뷰
1. 후기
- 음성은 실질적인 소리를 나타내고,  음소는 인지적인 소리를 나타낸다는 것을 알았습니다. 음소는 일종의 약속이라 다른 소리임에도 동일한 표기로 나타낼 수 있는 것 같습니다. 또한 왜 사람마다 음색이 다른건지 궁금했는데, 우리의 목소리가 성대에서 비롯되어 여러 주파수가 겹쳐진 결과이며, 우리가 말하는 것은 기타가 소리를 내는 것과 비슷하다는 것을 배우고 난 뒤 그 이유를 알 수 있었습니다. 또 standing wave라는 개념은 처음 접해서 이해가 좀 어려워 알아보니 병의 입구에 바람을 불 때 특정 음으로 울리는 것이 병 안의 공기가 특정 주파수에서 standing wave를 만든 결과라는 것을 알았습니다. 이번 강의를 통해 발성과 소리에 관한 기초 지식을 알고 나니 일상 속에서 흘려들었던 소리들도 분석하면 재미있는 결과가 나올 수 있다는 것을 느꼈으며, 소리가 가지는 특성 정보를 반대로 구현하는 것이 현대 인터넷 미디어가 소리를 재생하는 원리가 아닐까 하고 유추해 볼 수있었습니다. 

2. 수업 중후반부에 배웠던 formant 1,2의 Hz와 혀의 높낮이, 전/후 위치의 관계를 상호작용 가능한 html 파일로 만들어 보았습니다. 해당 파일 업로드해두었습니다. F2의 Hz가 높아지면 혀가 앞으로 가는데, F1의 Hz가 높아지면 혀가 낮아지는지 궁금했습니다. 그 이유를 알아본 결과 F1은 입의 열림 정도와 관련된 수치라 입이 크게 열려 혀가 낮아지면 성도의 첫 번째 공명 주파수가 높아져 F1의 Hz가 높아지는 것이며, F2는 혀 앞쪽의 공명 공간의 길이와 관련된 수치라 혀가 앞으로 가면 그 공명 공간의 길이가 짧아져 F2의 Hz가 높아지는 것이라고 합니다. 단순히 주파수의 크기가 아니라 공간과 공명의 개념으로 생각하면 헷갈리지 않을 것 같습니다.

# 프로젝트 사이트 링크 : m0on1115.com

# 프로필
2024130871 신희문 (*계정을 변경했습니다.)

# 현재 버전 - beta 1.0
- OPENAI API 연결 완료
- Vercel 활용 백엔드 연결 및 도메인 연결 완료
- 디자인 일부 수정

# 앞으로의 방향
- 현재 안정화를 위해 API 토큰을 과다 사용중이므로, 이를 개선하고자 함
- 지원하는 학과 및 자격증 목록 추가
- 아이템 추천 로직 고도화
- 모바일 페이지 개선
